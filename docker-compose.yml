services:
  bolha-scraper:
    build: .
    container_name: bolha-scraper
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
    command: ["node", "dist/index.js"]
